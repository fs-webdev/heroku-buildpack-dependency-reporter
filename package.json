{
  "name": "heroku-buildpack-dependency-reporter",
  "version": "0.1.0",
  "description": "Uploads applications dependencies to S3",
  "main": "lib/upload.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "directories": {
    "lib": "lib"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1097.0"
  }
}
